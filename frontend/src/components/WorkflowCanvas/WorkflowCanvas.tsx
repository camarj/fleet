/**
 * WorkflowCanvas — STUB (Phase 2).
 *
 * In Phase 2 this becomes a React Flow canvas (@xyflow/react 12) where the user
 * wires agents into a workflow the orchestration engine executes. Phase 1 ships
 * only this placeholder so the layout and navigation already exist.
 */

export function WorkflowCanvas(): React.JSX.Element {
  return (
    <div className="canvas-stub">
      <h3>Workflow Canvas</h3>
      <p>Phase 2 — node/edge canvas with React Flow (@xyflow/react 12). Not built yet.</p>
    </div>
  );
}
