# file: dependencies (the Contract lives in the sibling Scaffolding repo for now)
# must be linked, not copied, so a single source of truth is preserved.
link-workspace-packages=true
prefer-workspace-packages=true
