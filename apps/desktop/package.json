{
  "name": "@inteliside/gateway-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tauri v2 desktop shell (macOS first). Launches the Core as a sidecar and loads the frontend in the WebView.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:sidecar": "node scripts/build-sidecar.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "esbuild": "^0.27.0",
    "postject": "^1.0.0-alpha.6"
  }
}
