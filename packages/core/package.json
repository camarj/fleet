{
  "name": "@inteliside/gateway-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gateway Core — the brain. Speaks Flue to agents and exposes a WebSocket API to the frontend.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "gateway-core": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "GATEWAY_NO_AUTH=1 tsx watch src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "tsx test/flue.test.ts && tsx test/session-history.test.ts && tsx test/deploy-log-db.test.ts && tsx test/model-override.test.ts && tsx test/orchestrator.test.ts && tsx test/workflow.test.ts && tsx test/token.test.ts && tsx test/dokploy.test.ts && tsx test/pricing.test.ts && tsx test/usage-summary.test.ts"
  },
  "engines": {
    "node": ">=22.18"
  },
  "dependencies": {
    "@flue/sdk": "0.10.1",
    "@inteliside/gateway-converter": "workspace:*",
    "ws": "^8.18.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@flue/cli": "0.10.1",
    "@flue/runtime": "0.10.1",
    "@types/express": "^5.0.6",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.12",
    "express": "^5.2.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
